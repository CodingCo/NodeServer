//
   Created by Robert on 29-10-2014.
extends layout
block content
    .container-fluid
        .well
            p I am a navigation
        .panel.panel-default
            .panel-heading Employee
            .row
                .col-md-12
                    table.table.table-striped
                        thead
                            tr
                                th
                                th Info
                        tbody
                            if customer
                                script.
                                    var customer = !{JSON.stringify(customer)}
                                    var str = "";
                                    for (var item in customer) {
                                        var name = customer[item];
                                        var prop = item;
                                        str += "<tr>";
                                        str += "<td>" + prop;
                                        str += "<td>" + name;
                                        str += "</tr>";
                                    }
                                    $('tbody').html(str);
                            else
                                script.
                                    var employee = !{JSON.stringify(employee)}
                                    var str = "";
                                    for (var item in employee) {
                                        var name = employee[item];
                                        var prop = item;
                                        str += "<tr>";
                                        str += "<td>" + prop;
                                        str += "<td>" + name;
                                        str += "</tr>";
                                    }
                                    $('tbody').html(str);
        .row
            .col-md-1
                button.btn.btn-default.btn-lg(type='button' id='backbtn')
                    span.glyphicon.glyphicon-chevron-left
                    |  Back

        script.
            $(".container").addEventListener("click", function() { alert("alert");});
            //$('#content').load('orders');